The portrait boxes are too small, the portraits can't be seen properly. There should only be 3 large portrait boxes for each team, when its banning phase, the portrait boxes will display the picked characters. However when selection phase starts, the portrait display of the banned characters will be removed but they are still cant be picked on the icon list, then the selected picks on the selection phase will be displayed on the large portrait boxes just like in the reference interface.
Make it so that the user can still pick available characters until the user clicks lock pick, this is to enable the user to still click available icons before lock picking. Make sure that when the user free click icons, the corresponding portrait also updates on the specified portrait box.
Instead of display the tournament begins at the bottom of the page, create a modal that displays it in the center.